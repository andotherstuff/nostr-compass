# Human editorial overrides — Newsletter #37

## walls.rip (added 2026-08-26)

The editor explicitly requested that `https://walls.rip/` be added to the still-unpublished issue through the newsletter-fix path. Preserve the source-grounded Ghost Chat coverage in Top Stories across any resumed Writing or Assembly stage. The Nostr Relay test passes because the client publishes and subscribes to kind 1 events on public Nostr relays. The So What test passes because the implementation demonstrates both the utility and the metadata/interoperability limits of app-specific encrypted payloads over relay transport.
