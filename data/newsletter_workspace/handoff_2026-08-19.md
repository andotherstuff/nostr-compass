# Stage 8 handoff — Newsletter #36 (2026-08-19)

**Draft:** `content/en/newsletters/2026-08-19-newsletter.md` (`draft: true`)
**Draft PR:** https://github.com/andotherstuff/nostr-compass/pull/135
**Branch:** `newsletter/2026-08-19`, one commit on top of `origin/main` at `06046df`
**Workspace:** `/opt/data/compass-worktrees/2026-08-19`

This run was executed by hand from the Hermes workspace. The `compass-tuesday-intake` cron and
every downstream Compass job are paused because the OpenRouter writing lanes return HTTP 402
`Insufficient credits` (`/opt/data/KIMI_OPENROUTER_PAUSE.md`, 2026-08-17), so no Kanban worker
could claim the pipeline task. All eight stages ran in order and each wrote its artifact.

## Contents

- 6 Top Stories: Amber, Cambium, Citrine, Vector, Sonar, Nostria
- 9 Tagged Releases: MDK, Divine Mobile, ClipRelay, Bark, Bray with Toll Booth, NoorNote,
  Mostro, NYM, Morganite
- 3 Newly Discovered: Nail, nwc-kit, Glow
- 7 In Development: Amethyst, nostrord, nostream, rust-nostr, NDK, Nostter, Zap Cooking
- Protocol and Spec Work: 3 NIP proposals, 1 merged Marmot MIP, 1 merged and 2 opened Concord
  proposals; BUD, NAP, and Gamma Markets recorded quiet
- 2 NIP Deep Dives: NIP-58 (Badges) and NIP-22 (Comments), each with a relay-recovered event

6,382 words, 6 H2 sections, 30 H3 subsections, 165 distinct external links.

Nail was added on 2026-08-18 after the owner flagged it as missing; see
`review_human_2026-08-18T1600Z.md` for the root cause, the pipeline fix in PR #136, and the
gates re-run afterwards.

## Topic pages

No new topic page was required. Twenty-seven existing pages received a `Mentioned in` entry for
this issue: `blossom` plus NIP-05, 07, 10, 13, 17, 22, 27, 29, 42, 43, 44, 45, 46, 47, 51, 55,
58, 59, 5A, 65, 66, 70, 73, 78, 86, and 98. Every fragment was validated against the rendered production
HTML.

## Review swarm

| Reviewer | Artifact | Gate |
|---|---|---|
| LinkChecker | `review_links_2026-08-19.md` | PASS (165/165 external links HTTP 200) |
| ClaimCheck | `review_claims_2026-08-19.md` | PASS (23 release audits, 40/40 PRs verified MERGED) |
| ProseReview | `review_prose_2026-08-19.md` | PASS (anti-slop 100/100, zero violations) |
| TopicAudit | `review_topics_2026-08-19.md` | PASS (build exit 0, 27/27 backlinks resolve) |
| ContinuityValueCheck | `review_continuity_2026-08-19.md` | PASS (checker PASS, 3 prior issues read) |

Consolidated in `review_log_2026-08-19.md`, which ends in an evidence-bearing `GATE: PASS`
after two review rounds and zero outstanding fix-list items.

## Identity and outreach

`bun scripts/publish.ts --no-inject` resolved **18 projects**, **0 missing**, **2
researched-unresolved**. `bun run check:npubs` reports 324 entries, 166 valid unique pubkeys,
0 errors, 2 pre-existing legacy warnings.

Newly bound this issue: Cambium, Bark, Toll Booth, and nwc-kit to the forgesworn key already
verified for Bray (Cambium's README publishes the key directly; `forgesworn.dev`'s NIP-05 maps
`_` and `darren` to it), and NYM to its lead developer's key with the role recorded.

Outreach was dry-run first and then sent:

- Plan `publish/out/dm-outreach-36-plan.json`: 16 planned, 16 unique npubs, 0 failed, 1 skipped.
- Receipt `publish/out/dm-outreach-36.json`: `really_send: true`, **16 sent, 0 failed**, 15 over
  NIP-17 gift wrap and 1 over NIP-04 fallback, every sent row carrying a non-empty event id and
  `relaysOk` of 11 (10 for the NIP-04 recipient). The single skip is the configured `no_dm`
  exclusion for MDK/Marmot.
- Recipients: Amber, Amethyst, Bark (forgesworn, de-duplicated with Cambium, Toll Booth and
  nwc-kit), ClipRelay, Divine Mobile, Mostro, NDK, NoorNote, nostream, Nostria, nostrord, NYM,
  rust-nostr, Sonar, Vector, Zap Cooking.
- Message: the standard pre-publication review request pointing at PR #135, plus the Thursday
  16:00 UTC podcast invitation at the configured Riverside room.

## Open items for the reviewer

1. **Concord and Nostter have no verified npub.** Both are recorded under `unresolved:` in
   `data/npubs.yml` with the negative research: the concord-protocol repositories publish no
   project account, and Nostter's three README keys credit UI, logo, and illustration
   contributors while `nostter.app`'s NIP-05 endpoint returns an interstitial instead of JSON.
   Neither received a DM. If you hold verified keys, adding them before the Wednesday refresh
   would let a targeted follow-up reach them.
2. **Glow is introduced without an in-window Nostr change.** The link queue asked for it to be
   included; its GitHub work this week is Lightning and app-lock only, so the section describes
   its standing relay-backed wallet-label surface instead. Confirm that framing is what you
   wanted.
3. **Zapstore new-versus-update classification was unusable this run.** A fresh weekly worktree
   carries no `publishers_seen.yml`, so the fetcher recorded a 1202-record first-run baseline.
   No Zapstore-only launch is claimed in the issue. Worth fixing in the workspace resolver so
   the state file follows the worktree.
4. **Chama v6.0.0, Earthly, and NostrAppShell stayed MAYBE.** Reasons are in
   `triage_2026-08-19.md`; none are written into the issue.
5. **The owner sweep surfaced three more untracked Formstr repositories** while proving the fix:
   `better-nips`, `super-app`, and `common-packages`. All three currently have empty descriptions,
   so none was written up. They are candidates for next week's triage.
6. **Six tracked repositories point at GitHub owners that now return 404** (`blowaternostr`,
   `fr4nzap`, `jacany`, `madcato`, `nextdebug`, and one more). The sweep reports them as warnings.
   Worth a separate `projects.yml` cleanup pass.

## What has not happened

No merge, no deployment, no `draft: false`, no signing, and no kind 30023 or kind 1 broadcast.
The PR is open in draft state.

The issue now waits for your approval of this initial draft, then the Wednesday 14:00 UTC
all-source refresh and the 16:00 UTC publication window. Both of those crons are currently
disabled; see the Kanban notes on the `compass` board for what has to be re-enabled or run by
hand once the model lanes are funded.

**READY FOR HUMAN REVIEW**

GATE: PASS (Stage 8 complete; draft PR #135 open in draft state at one commit; 16/16 outreach DMs delivered with verified receipts; draft remains unpublished)
