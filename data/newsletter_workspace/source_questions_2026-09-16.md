# Compass #40 source questions

Checked: 2026-09-16T11:19:24Z

## Unresolved

None.

## Resolved during writing

- The Stage 4 Keycast link used the stale `keycastxyz/keycast` owner and returned HTTP 404. The same primary recap source named the canonical `marmot-protocol/keycast` release URL; that URL returned HTTP 200 and is used in the draft.
- Two freshly signed Zapstore release events were not yet indexed by njump. Their exact IDs were retained from the signed Stage 3 export and their Primal event pages returned HTTP 200, so the draft links those verified event pages rather than dead njump routes.
