# Stage 1 intake — Newsletter #36 (2026-08-19)

Input: `data/newsletter_workspace/link_queue.md`, copied into the weekly worktree by
`compass_weekly_workspace.py`. Two entries, both carrying a dated `Prep (verified …)` block,
so this pass revalidates the recorded fields instead of repeating completed prep.

## Queue entry 1 — Glow (2026-08-13T08:52:43Z)

- Submitted source: https://breez.technology/glow/
- Editorial note: "Apparently, Glow is using Nostr for some wallet metadata stuff, so add it
  to our projects and include for the next newsletter."

Revalidation:

- Canonical repositories confirmed: [breez/glow-web](https://github.com/breez/glow-web) (the
  PWA) and [breez/glow-app](https://github.com/breez/glow-app) (the native iOS/Android
  wrapper). Both resolve, both are public, neither redirects to a renamed repository.
- Nostr surface confirmed from primary source, not from the queue note. `src/services/passkeyService.ts`
  on the default branch primes a Nostr identity from the passkey credential and then
  lists and stores wallet labels as relay events (`Listing labels from nostr relays`,
  `Saving label to nostr relays`, plus dedup logic for "partial relay coverage can return the
  same label event twice"). This is a real Nostr surface, not a marketing claim.
- Dedup against `data/projects.yml`: Glow was absent. Added under `wallets` with
  `priority: medium` and a scope note restricting coverage to Nostr-relevant changes, matching
  the placement table for "LN wallet with Nostr … surface". No new top-level category invented.
- Not a rebrand: no prior Compass coverage of `glow-web`/`glow-app`/Glow, no GitHub redirect
  from an older name, and the repository history starts under the current name.
- **In-window activity gate: FAIL.** Every commit merged to `glow-web` after the #35 cutoff
  (`fcaff4e7`, `4dd934c5`, `13d6818f`, `1c8a71cd`, `3b289709`, `420ed13b`, `e5b9aab2`,
  `ac326eb0`, `446fa3e7`, `326f877f`, `d3b575fc`, `d6960826`, `15cea267`) is Lightning,
  on-chain, or app-lock UX. None of them touches an event kind, a NIP, or the relay path.
  Under the per-PR Nostr-surface gate none of this week's Glow work is newsletter material.
- Resolution: Glow is carried into Triage as a **Newly Discovered** project introduction, which
  is what the queue prep asked for, described by its standing Nostr surface rather than by an
  in-window changelog. Its Lightning work stays out of the prose.
- Identity: no dedicated project npub. `breez.technology` serves no `/.well-known/nostr.json`
  and `glow.money` does not resolve to one. Recorded as unresolved rather than guessed; see the
  Stage 8 outreach note.

## Queue entry 2 — Cambium (2026-08-14T10:44:59Z)

- Submitted source: https://github.com/forgesworn/cambium
- Editorial note: "For next week."

Revalidation:

- Already tracked in `data/projects.yml` under `signers`. No entry change needed.
- Description in the prep block still matches the repository: an Android NIP-55 signer that
  holds no private key material and proxies signing requests over NIP-46 to a companion
  Heartwood hardware signer.
- The prep block's recorded head (`984dfd3b`, 2026-08-14) is stale by design; the project
  shipped six tagged releases inside this issue's window (v0.3.5 through v0.4.3), so Triage
  gets the release set rather than a commit pointer.
- **In-window activity gate: PASS.** v0.4.0 adds NIP-55 website signing through validated
  `nostrsigner:` callbacks and fixes minimal-event signing; v0.3.6 fixes NIP-46 pairing against
  signers that answer `connect` by echoing the bunker secret; v0.4.1 through v0.4.3 bound the
  NIP-46 request queue under relay AUTH bursts. All four are Nostr-surface changes.
- Identity: `data/npubs.yml` records `# Cambium: no verified npub found`. Revalidated this run:
  `forgesworn.dev/.well-known/nostr.json` maps `_` and `darren` to
  `da19f1cd34beca44be74da4b306d9d1dd86b6343cef94ce22c49c6f59816e5bd`
  (`npub1mgvlrnf5hm9yf0n5mf9nqmvarhvxkc6remu5ec3vf8r0txqkuk7su0e7q2`), the same key already
  bound to Bray as a forgesworn project. Cambium, Bark, and Toll Booth are published from the
  same project site, so the Newsletter #36 additions bind them to that key with the existing
  Bray evidence and keep the maintainer alias de-duplicated to a single recipient.

## Queue archival

After this artifact was written, `data/newsletter_workspace/link_queue.md` was moved to
`data/newsletter_workspace/link_queue_archive_2026-08-19.md` inside the worktree, compared
byte-for-byte with `/opt/data/compass/data/newsletter_workspace/link_queue.md`, and the
identical shared inbox copy was moved to
`/opt/data/compass-inbox-archive/link_queue_2026-08-19.md` so next Tuesday cannot replay it.

## Auto-fetch input

Beyond the queue, Stage 2 runs in normal auto-fetch mode across all ten `fetch_all.sh` source
families. No user-supplied URL in this run failed to resolve.

GATE: PASS (2 queue entries revalidated; Glow added to `data/projects.yml` under `wallets` and routed to Triage as a Newly Discovered introduction; Cambium confirmed tracked with four Nostr-surface releases in window; zero unresolvable URLs)
