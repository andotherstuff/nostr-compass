# Handoff — Newsletter #31 (2026-07-15)

## Resume context

Resumed per user instruction from Stage 4 (Selection) through Stage 7 (Review Swarm), continuing from Stage 3 Triage which had already completed and GATE:PASS'd at 16:08 UTC. No API timeouts or 500s occurred during this run, so the retry-with-backoff instruction was never invoked. No further stalls occurred after resume; all stages completed in one continuous pass.

## Draft location

`content/en/newsletters/2026-07-15-newsletter.md`

- Frontmatter: `title: "Nostr Compass #31"`, `date: 2026-07-15`, `publishDate: 2026-07-15`, `draft: false`, `type: newsletters`.
- Note: `date`/`publishDate` are one day ahead of the system clock (today is 2026-07-14). This is expected for the weekly Wednesday-publish cadence; `hugo` will render the page automatically once the date arrives, or use `hugo --buildFuture` for local preview before then.

## Section item counts

| Section | Items |
|---|---|
| Lead stories | 5 |
| Tagged releases | 10 |
| Unreleased changes | 7 (+ 1 grouped "Also shipped" paragraph, 7 projects) |
| Newly tracked and discovered | 3 (+ 1 grouped "Also launching" paragraph, 3 projects) |
| Protocol work and NIP updates | 6 open PRs, 0 merged |
| NIP Deep Dive | NIP-15 + NIP-99 (paired), 3 subsections |

## NIP Deep Dive

**NIP-15 (Nostr Marketplace) and NIP-99 (Classified Listings)** — first appearance of both in the rotation history. Anchored to this week's Conduit Mono marketplace work (ephemeral guest checkout, orders-inbox rebuild). Full JSON examples included for both kind 30017/30018 (NIP-15) and kind 30402 (NIP-99).

## Topic pages touched

**Updated with new "Mentioned in" entries (no new pages created, all NIPs already had pages):**
nip-15, nip-99 (deep dive), nip-85 (Amethyst), nip-29 (message pinning PR), nip-66 (relay discovery restructure PR), nip-57 (Wired/TAO), nip-49 (Nostr WoT + Nostr Docs), nip-55 (Cambium), nip-47 (cdk), nip-46 (Vector + n_cord).

## projects.yml changes

Added **Cambium** (forgesworn/cambium) under `signers`, priority `medium` (Discovery-slot addition, never high on first appearance per standing rule).

## npub injection note for `bun scripts/publish.ts`

None of this issue's newly-tracked projects (OpenDiscord, Auditable Voting, Cambium, echoes, Dispatch, Linky) currently have entries in `data/npubs.yml`. `publish.ts` will skip NIP-27 npub injection for these mentions and warn to stderr; this is expected for first-appearance projects and not a blocker. All previously-tracked projects mentioned this issue (Vector, Sonar, Divine, Bitchat, MDK, n_cord, cdk, Coop Mobile, Nmail, Nostrord, Nostr WoT, Keep, Noscall, Kubo, Nostr Codex Phone, Amethyst, Zap Cooking, Kehto, Wired, TAO, Conduit Mono, Buzz, Nostr Docs, ngit-cli, Manent, NoorNote, Bray, Lumilumi, Earthly, lnbits) should already resolve normally against the existing npub database; not independently re-verified in this run.

## Infrastructure fix made during this run

Added `ignoreFiles = ['newsletter_workspace']` to `hugo.toml`. Hugo's data-directory loader was failing the full-site build (`unmarshal of format "" is not supported`) because it tried to parse the pipeline's own `data/newsletter_workspace/` markdown scratch files as structured data. This is additive and does not change any other site behavior. Recommend keeping this fix; without it, `hugo` will fail to build the entire site any week the workspace directory is present, which is every week per the Orchestrator's own workspace-retention policy.

## Review swarm final scores

All four reviewers (LinkChecker, ClaimCheck, ProseReview, TopicAudit) report **GATE: PASS**. One ProseReview fix was applied (a rhetorical question in the NIP Deep Dive intro rewritten to a declarative sentence) and re-verified clean. Full detail in `data/newsletter_workspace/review_log_2026-07-15.md`.

- 83/83 external links returned HTTP 200.
- 20/20 internal `/en/topics/` links resolve.
- 37/37 in-page anchor links resolve.
- `hugo --buildFuture` builds cleanly, 277 EN pages, no errors.
- Zero em dashes, curly quotes, exclamation marks, or banned hype phrases.
- No fabricated NIP numbers (the NIP-9A red flag from Selection was correctly avoided in the final prose).

## Outstanding items deliberately left for human review

- Not independently re-verified in this pass: npub-injection correctness for the ~29 previously-tracked projects mentioned (relies on existing `data/npubs.yml` entries from prior issues).
- `data/coverage_history.json` was not regenerated/appended in this run; that is normally a post-publish step per the documented pipeline and was intentionally left for Stage 8/9 publish tooling rather than done manually here.

## Status

**READY FOR HUMAN REVIEW.** This run stops here per the explicit instruction to halt before any Stage 8 publish action (branch creation, PR, `bun scripts/publish.ts`, or Nostr event signing/broadcast). No publish-side action has been taken.

GATE: PASS
